import { useReactiveVar } from '@apollo/client';
import React, { useEffect } from 'react';
import { routeTitleVar } from '../../../common/apollo';

export const GameHistoryInfo = () => {
  return (
    <div>
      <div>this is GameHistoryInfo component</div>
    </div>
  );
};
